<%= form_for(@shipping_option) do |f| %>
  <% if @shipping_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shipping_option.errors.count, "error") %> prohibited this shipping_option from being saved:</h2>

      <ul>
      <% @shipping_option.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :cost %><br />
    <%= f.text_field :cost %>
  </div>
  <div class="field">
    <%= f.label :cost %><br />
    <%= f.text_field :cost %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
